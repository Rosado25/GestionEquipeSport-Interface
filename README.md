# ⚽ GestionEquipeSport-interface

**GestionEquipeSport-interface** est une application web moderne conçue pour la gestion simplifiée d'équipes de football. Ce projet sert d'interface utilisateur (Front-end) et interagit avec un écosystème de deux API distinctes.

---

## 📝 Description

Cette application centralise l'organisation de votre structure sportive. Elle permet de visualiser et de gérer les effectifs, les calendriers de matchs et les performances, le tout via une interface fluide et responsive.

L'architecture repose sur la communication avec deux serveurs distincts :

1. **API d'Authentification :** Sécurise l'accès (connexion, gestion des sessions).
2. **API Principale :** Coeur de métier (données des joueurs, statistiques des équipes, résultats des matchs).

> [!IMPORTANT]
> Ce dépôt contient uniquement le code de l'interface client. Les API ne sont pas incluses ici.

---

## ✨ Fonctionnalités

* **Sécurisation :** Authentification complète (Login / Logout).
* *Note : L'inscription est actuellement gérée en interne/via API.*


* **Gestion Sportive :** Administration complète des équipes et des fiches joueurs.
* **Calendrier :** Consultation des matchs à venir et saisie des résultats.
* **Expérience Utilisateur :** Interface intuitive et **100% Responsive** (compatible mobile, tablette et desktop).

---

## 🏗️ Architecture du Système

L'application fonctionne comme un hub centralisant les données provenant de deux sources :

---

## ⚠️ Remarques Importantes

* **Dépendances :** Pour que l’interface soit fonctionnelle, les deux API doivent être lancées et configurées pour accepter les requêtes provenant de ce Front-end (CORS).

---

*Projet développé dans le cadre de academique.*

---

Souhaites-tu que j'ajoute une section technique plus précise (comme les technologies utilisées : React, Vue, Tailwind, etc.) ou que je détaille la structure des dossiers ?
